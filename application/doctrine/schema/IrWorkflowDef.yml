IrWorkflowDef:
    listeners: [ReplaceInvalidCharactersListener, XssListener, IndexListener]
    
    columns:
        name: 
            type: string(255)
        description: 
            type: string
            extra: 
                purify: html

    actAs:
        Timestampable:
            created:
                name: createdTs
                type: timestamp
            updated:
                name: modifiedTs
                type: timestamp
