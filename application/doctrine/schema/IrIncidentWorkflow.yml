IrIncidentWorkflow:
    columns:
        name: 
            type: string(255)
            extra:
                purify: plaintext
        description:
            type: string
            extra:
                purify: html
        cardinality: 
            type: integer
        status:
            type: enum
            values: ['queued','current','completed','reopened']
        completeTs: 
            type: timestamp
        comments:
            type: string
            extra: 
                purify: html
        attachment: 
            type: string
            extra:
                purify: plaintext

        incidentId: 
            type: integer
        roleId: 
            type: integer

    relations:
        # One to one:
        Incident:
            local: incidentId
            foreign: id
            foreignType: one
            foreignAlias: Workflow
        # One to Many:
        Role:
            local: roleId
            foreign: id
            foreignAlias: IncidentWorkflows
