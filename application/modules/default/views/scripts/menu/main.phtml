<?php
// This view script is only used for debugging and should not be invoked on production systems

if (Fisma::debug()) {
    var_dump($this->menubar);
} else {
    throw new Fisma_Zend_Exception("This view is only allowed in debug mode.");
}
