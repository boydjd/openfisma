<?php
echo $this->escape($this->searchBox($this->toolbarButtons), 'none');
Fisma_Format_Section::startSection("Report A Security Incident");
echo $this->escape($this->form, 'none');
Fisma_Format_Section::stopSection();
?>
<script type="text/javascript">
    $(function() {
        var assetRow = $("table.fisma_crud > tbody > tr:has(#assetInfo)"),
            piiRows = $("table.fisma_crud > tbody > tr:has(input[name=piiMobileMedia], input[name=piiEncrypted], input[name=piiIndividualsCount])");
        assetRow.hide();
        piiRows.hide();
        $("input[name=assetInvolved]").change(function(ev) {
            if (Number($(ev.target).val())) {
                assetRow.show();
            } else {
                assetRow.hide();
            }
        });
        $("input[name=piiInvolved]").change(function(ev) {
            if (Number($(ev.target).val())) {
                piiRows.show();
            } else {
                piiRows.hide();
            }
        });
    });
</script>
