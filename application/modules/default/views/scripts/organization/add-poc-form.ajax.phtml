<table class='keyValues' width='100%'>
    <tr>
        <th scope='row'><label for='addPocAc'>User</label></th>
        <td>
            <?php echo $this->escape($this->userAc, 'none'); ?>
            <input type='hidden' id='addPocId' name='addPocId' />
        </td>
    </tr>
    <tr>
        <th scope='row'><label for='addPocRole'>Role</label></th>
        <td>
            <select id='addPocRole' name='addPocRole'>
            <?php foreach ($this->roles as $role): ?>
                <option><?php echo $this->escape($role); ?></option>
            <?php endforeach; ?>
            </select>
            <script>
                $(function() {
                    var select  = $('select#addPocRole'),
                        html    = select.html();
                    select.button().html(html);
                });
            </script>
        </td>
    </tr>
    <tr>
        <td><?php echo $this->escape($this->submitButton, 'none'); ?></td>
        <td><i>Submitting this form will refresh the page; unsaved changes will be discarded.</i></td>
    </tr>
</table>
