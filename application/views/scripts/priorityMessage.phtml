<?php if (count($this->messages)): ?>
<script type="text/javascript">
<?php foreach ($this->messages as $severity=>$messages):
        if (count($messages)) :
            foreach ($messages as $message) : ?>
    message('<?=$this->escape($message, 'javascript') ?>', '<?=$this->escape($severity, 'javascript') ?>');
<?php       endforeach;
        endif;
    endforeach;?>
</script>
<?php endif; ?>
